{
  "name": "log-replay",
  "version": "1.0.0",
  "description": "Replay Log files",
  "main": "log-replay.js",
  "scripts": {
    "reset": "rm -f ./data/replicated-logs/*.log",
    "empty": "truncate -s 0 ./data/replicated-logs/*.log",
    "start": "cd ./scripts && node replay.js"
  },
  "keywords": [
    "Log",
    "File",
    "Replay"
  ],
  "author": "Matt Cole",
  "license": "ISC",
  "dependencies": {
    "minimist": "^1.2.8"
  }
}
